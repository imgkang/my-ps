{
  "name": "mypm-server",
  "version": "0.1.0",
  "private": true,
  "description": "My PM 백엔드 — 데이터 저장(SQLite) + 주가 프록시 + 종목 검색 + 푸시 알림",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "import": "tsx src/scripts/import-bundle.ts",
    "load-tickers": "tsx src/scripts/load-tickers.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/static": "^8.0.3",
    "apns2": "^12.2.0",
    "better-sqlite3": "^11.8.1",
    "fastify": "^5.2.1",
    "firebase-admin": "^13.0.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.5",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
